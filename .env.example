# Optional: Set the logging level (e.g., 'trace', 'debug', 'info', 'warn', 'error', 'fatal')
# Defaults to 'error' if not set.
# LOG_LEVEL=debug

# Optional: Specify the full path to the ffmpeg executable if it's not in the system PATH.
# Required for sending/converting audio messages (like voice notes).
# Example Linux/macOS: FFMPEG_PATH=/usr/bin/ffmpeg
# Example Windows: FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
# FFMPEG_PATH=

# Optional: Specify the full path to the ffprobe executable if it's not in the system PATH
# or located alongside ffmpeg. Also used for media processing by the underlying library.
# Example Linux/macOS: FFPROBE_PATH=/usr/bin/ffprobe
# Example Windows: FFPROBE_PATH=C:\ffmpeg\bin\ffprobe.exe
# FFPROBE_PATH=

# Example (if needed later for Puppeteer):
# PUPPETEER_EXECUTABLE_PATH=/path/to/your/chrome/or/chromium
